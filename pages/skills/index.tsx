import type { NextPage } from 'next';
import Head from 'next/head';
import Layout from '../../component/Layout';

const Skills: NextPage = () => {
    return (
        <Layout><div>Skills</div></Layout>
    );
 };

 export default Skills;